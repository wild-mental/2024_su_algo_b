import sys

x,y = map(str, sys.stdin.readline().split())

X = x[::-1]
Y = y[::-1]

z = str(int(X)+int(Y))
print(int(z[::-1]))

---------------------------------------------
문제풀이

[ : : -1] 는 배열의 처음부터 끝까지 역순으로 바꾼다는 의미이며
배열에는 str형으로 집어 넣어 문제를 풀었다.
